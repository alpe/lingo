apiVersion: kubeai.org/v1
kind: Model
metadata:
  name: opt-125m-cpu
spec:
  features: [TextGeneration]
  url: hf://facebook/opt-125m
  engine: VLLM
  minReplicas: 0
  resourceProfile: cpu:1:2 # group size=2
  # Scale down immediately after autoscaler window drops.
  scaleDownDelaySeconds: 0
